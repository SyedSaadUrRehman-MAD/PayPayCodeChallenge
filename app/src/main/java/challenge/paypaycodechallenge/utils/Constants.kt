package challenge.paypaycodechallenge.utils

class Constants {
    companion object{
        const val DB_NAME: String="currency_db"
        const val KEY_RATES_SAVED="rates_saved"
        const val KEY_CURRENCY_SAVED="currencies_saved"
        const val KEY_LAST_REFRESHED="last_refreshed_timestamp"
        const val KEY_LAST_SELECTED ="last_selected_currency"
    }
}